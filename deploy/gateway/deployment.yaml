apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexigo-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      service: lexigo-gateway
  template:
    metadata:
      labels:
        service: lexigo-gateway
    spec:
      containers:
        - name: gateway
          image: lexi-go/gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080