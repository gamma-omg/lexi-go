apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lexigo-auth-db
spec:
  serviceName: "lexigo-auth-db"
  replicas: 1
  selector:
    matchLabels:
      app: lexigo-auth-db
  template:
    metadata:
      labels:
        app: lexigo-auth-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "{{ .Values.auth.db.user }}"
            - name: POSTGRES_PASSWORD
              value: "{{ .Values.auth.db.password }}"
            - name: POSTGRES_DB
              value: "{{ .Values.auth.db.name }}"
          volumeMounts:
            - name: lexigo-auth-db-storage
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: lexigo-auth-db-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
