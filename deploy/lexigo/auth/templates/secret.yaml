apiVersion: v1
kind: Secret
metadata:
  name: lexigo-auth
  namespace: lexigo
type: Opaque
stringData:
  JWT_ACCESS_SECRET: |-
    {{ .Files.Get .Values.auth.jwt.access.privateKey }}
  JWT_REFRESH_SECRET: |-
    {{ .Files.Get .Values.auth.jwt.refresh.key }}
  OAUTH_GOOGLE_CLIENT_ID: "{{ .Values.auth.oauth.google.clientID }}"
  OAUTH_GOOGLE_CLIENT_SECRET: "{{ .Values.auth.oauth.google.clientSecret }}"
