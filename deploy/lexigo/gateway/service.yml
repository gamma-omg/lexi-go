apiVersion: v1
kind: Service
metadata:
  name: lexigo-gateway
  labels:
    app: lexigo-gateway
spec:
  selector:
    app: lexigo-gateway
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080