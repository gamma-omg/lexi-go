apiVersion: v1
kind: Service
metadata:
  name: lexigo-words
  labels:
    service: lexigo-words
spec:
  selector:
    service: lexigo-words
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080