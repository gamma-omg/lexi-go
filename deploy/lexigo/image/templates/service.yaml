apiVersion: v1
kind: Service
metadata:
  name: lexigo-image
  labels:
    app: lexigo-image
spec:
  selector:
    app: lexigo-image
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.image.http.listenPort }}
      targetPort: {{ .Values.image.http.listenPort }}
