apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexigo-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lexigo-gateway
  template:
    metadata:
      labels:
        app: lexigo-gateway
    spec:
      containers:
        - name: gateway
          image: "{{ .Values.container.image }}:{{ .Values.container.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
